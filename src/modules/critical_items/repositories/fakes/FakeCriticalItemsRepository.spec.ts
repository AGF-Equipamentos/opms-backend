import FakeCriticalItemsRepository from './FakeCriticalItemsRepository';

let fakeCriticalItemsRepositoryTest: FakeCriticalItemsRepository;

describe('fakeCriticalItemsRepository', () => {
  beforeEach(() => {
    fakeCriticalItemsRepositoryTest = new FakeCriticalItemsRepository();
  });
  it('', () => {});
});
